# =======================================================
# 核心配置：自动处理换行符
# =======================================================
# 设置默认行为：Git 会在提交时转换为 LF，检出时根据操作系统自动转换
* text=auto

# =======================================================
# 源代码与配置文件 (强制认为是文本)
# =======================================================
*.java      text diff=java
*.xml       text
*.yml       text
*.yaml      text
*.properties text
*.json      text
*.sql       text
*.md        text
*.txt       text
*.html      text
*.css       text
*.js        text

# =======================================================
# 脚本文件 (关键配置！)
# =======================================================
# Shell 脚本必须强制使用 LF，否则在 Linux/Mac 上无法执行
# 特别是 mvnw (Maven Wrapper)
*.sh        text eol=lf
mvnw        text eol=lf

# Windows 批处理脚本必须强制使用 CRLF
*.bat       text eol=crlf
*.cmd       text eol=crlf
mvnw.cmd    text eol=crlf


# =======================================================
# 二进制文件 (禁止 Git 尝试转换换行符或进行文本 Diff)
# =======================================================
# Java 编译产物 & 归档
*.jar       binary
*.war       binary
*.ear       binary
*.class     binary

# 图片与文档
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.ico       binary
*.pdf       binary
*.doc       binary
*.docx      binary
*.xls       binary
*.xlsx      binary

# 字体
*.ttf       binary
*.eot       binary
*.woff      binary
*.woff2     binary